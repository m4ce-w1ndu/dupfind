#include "dedup/actions/executor.hpp"

namespace dedup {
namespace actions {

bool ActionExecutor::execute(const DuplicateGroup& group, ActionType action) {
    return false;
}

} // namespace actions
} // namespace dedup
