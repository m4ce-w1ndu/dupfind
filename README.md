# dupfind

Fast Duplicate File Finder with Smart Deduplication and Cache support.

## Project Structure

- `include/dedup/`: Public library headers.
- `src/`: Library implementation source files.
- `cli/`: Command-line interface entry point.
- `CMakeLists.txt`: Build configuration.
